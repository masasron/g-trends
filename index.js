'use strict'

module.exports = {
    Normalize: require('./src/Normalize'),
    SearchProviders: require('./src/SearchProviders'),
    ExploreTrendRequest: require('./src/ExploreTrendRequest'),
    DownloadTrendRequest: require('./src/DownloadTrendRequest')
}